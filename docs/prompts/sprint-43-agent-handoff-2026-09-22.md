# Sprint 43 expanded implementation-agent handoff

**Mission:** implement the focused [Sprint 43 parent #77](https://github.com/grounded-play/hunker-bunker/issues/77) and [playthrough remediation plan](../planning/sprint-43-playthrough-remediation-2026-09-22.md) against the actual repository, with incremental reviewed PRs and reproducible evidence. Do not rewrite the game by treating the user's notes as a specification for new unconnected systems. Preserve existing working systems and prove every reported symptom or scope change.

## Start here: baseline, logs and repository ownership

1. Pull `grounded-play/hunker-bunker`, confirm default branch `mothership` and reviewed base `48b144bdfec1e4f9492f5cf6884abaa2f345b925`; inspect **latest HEAD again** before changing code because this handoff may have aged. Use a feature branch per coherent issue, not direct edits to `mothership`. Search open PRs/issue comments before duplicate work.
2. **First task, ask for the playtest log that was just made.** It was not attached to the planning conversation. Ask the player for the file or exact hosted filename, build SHA/version, OS/Steam Deck or desktop, mode, resolution and approximate timestamps. In co-op ask for paired host and guest captures. If authorized credentials are available locally, follow `docs/session-log-review-runbook.md` and `npm run logs:fetch -- --latest 5` / `--name`; never ask them to paste `HB_LOG_UPLOAD_TOKEN` and never commit it. Run `npm run logs:analyze -- logs/<actual filenames>.json`, inspect original event rows and deduplicate progressive uploads of one session. Note what is proven, what is user observation only and what is missing instrumentation. Commit a concise redacted dated report for #86, **not** bulky raw logs.
3. Treat the uploaded Armory screenshot and the user's observed focus/clip/visual problems as valid manual bug reports, but don't call a cause verified until reproduced. Historic `docs/reports/hosted-session-log-review-2026-09-11.md`, `docs/reports/steam-deck-ui-and-multiplayer-bug-remediation-2026-09-11.md` and Sprint 41 release notes describe older work, not today's acceptance.
4. Map each symptom to the existing code paths below. Draft a short current-state audit per focused issue (implemented today / observed break / evidence gap / proposed smallest fix), then implement/test. Do not claim co-op completely broken without a route and paired capture. Update `PRODUCT_STATE.md` only when new code and actual acceptance evidence change its truth.

## S43-01 / #78 — safe account–campaign–run persistence (P0)

Audit `main.js:startNewTacticalRunFlow()`, `src/profile.js:clearSaveData()` (currently clears all `hb_*` records), `src/arcState.js`, `src/achievements.js`, `src/itemOwnership.js`, `src/loadout.js`, `src/steamCloudSaveBridge.js`, `src/blackBox.js` and each dossier/story owner. Define a versioned save contract: lifetime identity/owned inventory/earned unlocks/dossier discoveries/career stats; active campaign story/linchpins/attempts/deaths/days until victory; per-attempt world/black-box/checkpoint; transient multiplayer state. Implement explicit New Campaign, Retry/Next Attempt, Continue and destructive Reset All. Ensure Dossier emits/persists earned updates and survives New Campaign while campaign story points reset only on that action. Reload the app between assertions; a stable in-memory profile is not persistence. Preserve verified Steam inventory as an external entitlement source; never convert QA local grants into purchased ownership. Build legacy-save migration and loss-free Steam Cloud A/B conflict tests; ensure no duplicate rewards. Write tests for two failed attempts then victory, next campaign, crash/restart, old saves and real full reset.

## S43-02 / #79 — Archives and achievement/unlock experience (P1)

Inspect `main.js:buildArchiveModal/openArchiveLogDetail`, `src/storyArchive.js`, `src/achievements.js`, actual `index.html` hierarchy, CSS overflow rules and `src/inputActions.js` focus registration. Design a discoverable Records surface or related tabs with Lore, Dossier, Endings and Achievements/Unlocks using existing stores only. Eliminate running text, nested-scroll traps and overlapping cards; preserve portrait and long-form readable detail. Show contextual reward/source/progress without inventing unlocks, and reset only campaign-local flags. Test long non-English strings, disabled entries and Back focus at 1280×800 and 1080p, with in-game and Homebase entry points.

## S43-03 / #80 — Fab Bay and 13-recipe integrity (P1)

`src/fabricator.js:FAB_RECIPES` currently has **13: 6 weapon, 2 charm, 5 module**. Validate inventory output/equip/state/asset for **every one**, not just array length. `src/bank.js` holds `foundryActivated` and currency; fabricator stores queues/results. Ensure activation costs and bank Tech/Coin/Med, queued prints and durable output remain coherent on save/relaunch, death/retry and Steam Cloud. Add QA/debug-only Foundry and resource unlock for demo-season testing, not a production entitlement or public exploit. Decide explicitly if the new campaign resets the base and whether permanent schematics stay account-owned. Test mid-print, repeated claim, interrupted writes, balance preservation and gate visibility.

## S43-04 / #81 — Homebase hero preview and class-swapping (P1)

Audit `src/scoutHeroPreview.js`, `main.js:renderHomebaseConsole`, `src/armoryScene.js:loadOperatorModel`, 3D overlay loading/idle mixer and asset telemetry. Reproduce cold and warm 30-swap Scout/Tank/Engineer scenarios. Don't throw away the last good rendered class before async replacement is ready, or let stale async generations win; cache/prewarm safe rigs/animations, account for disposal/memory and handle rig fallback per `docs/planning/armory-character-rig-readiness-2026-09-22.md`. Show frame/class/abilities on Homebase; remove claims of displayed armament where the gun is not rendered. Detailed equipment belongs in Armory. Benchmark fixed route on desktop and physical Deck, capturing p95 switch time, long tasks and visible continuity.

## S43-05 / #82 — Armory information architecture, socket visuals and charm cords (P1)

Inspect `src/armoryUi.js`, `src/armoryScene.js`, `src/charmSockets.js`, `src/operatorEquipmentSockets.js`, `src/data/equipmentDefinitions.js`, markup/CSS and `docs/planning/equipment-effects-and-visual-sockets-plan-2026-09-21.md`. Correct overclock card overflow without illegible microtype. Move **Operator Polish** selection to the right-side Operator Exosuit Rig; make Live Stage Preview an honest complete fitted-loadout summary showing class, weapon, sheen/finish, charm, both overclocks and effects/trade-offs, chassis, patch, polish, HUD and voice where present. Remove redundant/conflicting descriptions. Wearables need rig/item-specific bone/scale/rotation calibration for Scout/Tank/Engineer and custom rig fallbacks; prevent attachment occlusion behind the player. Replace hovering/misplaced charm with a short authored cord/loop mounted at a safe per-weapon archetype gun anchor, with a consistent free-end charm socket and item-specific transform. Reuse mapping for gameplay and replicated players; never bind a wearable to a gun or alter weapon hitbox/sightline. Screenshot each 5-archetype×3-class calibration at 1280×800, include actual animation poses and overflow/occlusion checks.

## S43-06 / #83 — deployment stats, active goals and single recoverable Black Box (P1)

Use `src/multiplayerLobby.js` Solo/Daily/Co-op selection, `main.js`, achievements/campaign save model and season objective stores. Render meaningful career vs campaign vs current attempt: runs, deaths/lives lost, days elapsed, depth, story point/next level, recovered/lost/pending Black Boxes and current loadout. Daily Ops already has a panel/six goals in source; add selected, active Seasonal and mode-eligible goals for Solo and Co-op, with personal/squad ownership clearly labeled. Keep current selection after leaving/re-entering deployment.

`src/blackBox.js` and its unit test already implement replacement of the **active data entry** on the second death while retaining historical archive. Verify world integration in `src/threeGame.js`: a second death before retrieval removes old marker/interactable salvage; only newest salvage is recoverable, even after crash checkpoint reconciliation. Do not delete career death records when forfeiting a box. Add direct world-integration tests, not a duplicate store.

## S43-07 / #84 — Steam Deck focus and visual reachability (P0)

Use `docs/menu-input-navigation-contract.md`, `src/inputActions.js` ordered focus roots, native Steam Input and browser fallback mappings, `tests/e2e/menu-reachability.spec.js`. Build deterministic spatial focus traversal with D-pad/left stick and arrows/WASD through Homebase hero tabs, nested Armory pickers/overclocks/polish, Archives, Fab Bay and every Deployment card. Enter/A confirms; Esc/B returns to the invoker; LB/RB tabs; never require right-stick mouse emulation. Confirm overlays/modal traps, hidden-disabled exclusions, long-string wrapping, selected value behavior and 1280×800 no-overlap. Execute and video-record a **physical** controller-only full path including suspended/resumed build, dead/retry and results. Don't close #53 or #45 on unit tests alone.

## S43-08 / #85 — diagnose co-op, not a guessed defect (P0)

From two real Steam-installed clients on identical build, record host and guest logs and screen captures of auth, relay, create/join/invite, ready, synchronized deploy, safe spawn, remote class/equipment appearance, combat/objective synchronization, reconnect/host migration where supported, extraction/results and cleanup. Compare `src/multiplayerLobby.js`, `src/gameController.js`, `src/steamLobbyClient.js`, `src/remoteLoadout.js`, `server/relay.js`, shared-world/planner tests. Explicitly distinguish Steam service issues from offline/LAN fallback. Reproduce exact failure, write regression test and minimal fix, then retest full route with both accounts; record timestamps, room/build/roles and link release #45.

## Required workflow and acceptance output

- One coherent branch/PR per tested area rather than an unreviewable monolith; link #77 plus the affected focused issue. Prioritize #86 evidence, #78 save safety, #84 Deck and #85 co-op first; the rest can proceed after the save boundary is explicit.
- For each issue deliver a **before** reproduction, changed modules, migration/data implications, deterministic unit and Playwright assertions, localized UI implications, measured **after** and a human screenshot/video where visuals or physical hardware matter. Only mark accepted when the stated environment was actually exercised.
- Run `npm test`, `npm run lint`, `npm run presubmit`, `npm run i18n:audit` and focused E2E routes including `tests/e2e/menu-reachability.spec.js`. Do not copy the old 3,660-test number as a new run. Keep all seven locales and Steam Deck/desktop viewport budgets in scope.
- On clean pass, update [the Sprint 43 plan](../planning/sprint-43-playthrough-remediation-2026-09-22.md), a redacted date-stamped report in `docs/reports/`, relevant issue checklists, `PRODUCT_STATE.md` with evidence and #45 acceptance gate only after real hardware/two-account proof. Don't publish store-facing claims before those sign-offs.
- If the log is unavailable, proceed on self-contained reproducible code/UI work, explicitly mark missing evidence and request the capture; do **not** manufacture timestamps, claim packaged acceptance, or silently dismiss player-observed bugs because old automated tests passed.
